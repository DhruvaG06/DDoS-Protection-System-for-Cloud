# OneChance Dashboard (Phase Placeholder)

This directory is designated for the **React Dashboard** to be built in subsequent phases.

## Objectives
- Real-time traffic anomaly visualization.
- Three-tier action breakdown (`ALLOW`, `CHALLENGE`, `BLOCK`).
- Real-time autonomous recovery timeline (**Detect → Decide → Mitigate → Isolate → Recover → Verify**).
- WebSocket live connection to `ws://localhost:8000/ws/telemetry`.
